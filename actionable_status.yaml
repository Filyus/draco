# Current actionable implementation status for the Rust codebase.
#
# Scope:
# - Keep only items that still need code changes.
# - Do not mirror implementation_state.yaml or list completed work.
# - Remove items as soon as they are fixed and covered by tests, then update implementation_state.yaml with the completed behavior.
# - Prefer concrete file/line evidence over broad percentages.

version: 1
last_reviewed: "2026-04-24"

items:
  - id: rust-drc-bitstream-coverage
    area: draco-core
    priority: medium
    status: open
    summary: "Rust DRC decoder needs a deliberate legacy bitstream support policy and implementation."
    evidence:
      - path: "crates/draco-core/tests/drc_existing_files_test.rs"
        note: "Current supported fixture decode path is effectively modern mesh v2.2+, point-cloud v2.3 KD-tree, and Rust-produced v1.3 sequential."
      - path: "crates/draco-core/tests/drc_existing_files_test.rs"
        note: "inventory_skipped_testdata_drc_fixtures snapshots 13 skipped fixtures by path, header version, geometry type, method, and reason; all current skips are UnsupportedBitstream."
      - path: "draco-version-tools/"
        note: "Local C++ encoder/decoder binaries exist for 0.10.0, 1.0.0, 1.1.0, 1.2.5, 1.3.0, 1.5.7, and current main for compatibility fixture generation and parity checks."
    impact: "Shipped/legacy .drc assets from older Draco releases remain outside the Rust decoder surface until we choose how far back compatibility should go and implement those layouts."
    next_steps:
      - "Decide the oldest Draco/bitstream version worth supporting for Rust decode; candidates in current fixtures include mesh v1.1/v1.2/v2.0/v2.1 and point-cloud sequential v1.1/v2.2/v2.3."
      - "Use the compiled C++ version tools to generate or inspect compatibility fixtures for candidate boundaries before committing to implementation scope."
      - "Implement the first selected legacy bitstream family and shrink inventory_skipped_testdata_drc_fixtures accordingly."
